import styled from '@emotion/styled';

export const Layout = styled.div`
  display: flex;
  max-width: 1400px;
  margin: 0 auto;
`;

export const Main = styled.div`
  position: relative;
  padding: 25px;
  flex-grow: 1;
  min-height: 100vh;
`;
