import styled from '@emotion/styled';

export const Layout = styled.div`
  display: flex;
`;

export const Main = styled.div`
  position: relative;
  padding: 25px;
  flex-grow: 1;
  min-height: 100vh;
`;
