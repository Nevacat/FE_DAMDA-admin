import styled from '@emotion/styled';

export const Layout = styled.div`
  display: flex;
  position: relative;
`;

export const Main = styled.div`
  padding: 25px;
  flex-grow: 1;
  min-height: 200vh;
`;
